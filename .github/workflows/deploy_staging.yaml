name: Deploy to Staging

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy_staging:
    name: Deploy to Staging
    uses: zooniverse/tove/.github/workflows/deploy_app.yaml@production-release
    with:
      commit_id: ${{ github.sha }}
      environment: staging
    secrets:
      creds: ${{ secrets.AZURE_AKS }}
  slack_notification:
    name: Slack notification
    uses: zooniverse/tove/.github/workflows/slack_notification.yaml@production-release
    needs: deploy_staging
    if: always()
    with:
      commit_id: ${{ github.sha }}
      job_name: Deploy to Staging / deploy_app
      status: ${{ needs.deploy_staging.result }}
      environment: staging
      title_link: "https://alice.preview.zooniverse.org"
    secrets:
      slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
